<%- layout("layouts/main") %>

<%- include("partials/title") %>

<div class="container">
  <% if (auth) { %>
  <hr>
  <h2>Logged in as <b><%=username%></b>.</h2>
  <hr>
  <% } %>

  <article class="text-justify">
    <p>
      Welcome to my Markdown blog, where words come alive through elegant simplicity. Built with Express, this blog combines the power of a robust web framework with the flexibility of Markdown, offering an intuitive writing experience.
    </p>
    <p>
      Immerse yourself in a world of creativity as you craft your thoughts using Markdown's lightweight syntax. No more distractions – just focus on your content. The Express-powered backend ensures smooth navigation and optimal performance while making your journey through posts.
    </p>
  </article>
  
  <% if (auth) { %>
  <hr>
  <h2>My posts</h2>
  <hr>
  <% if (posts.length) { %>
    <ul class="list-group p-2 bg-light rounded">
      <% for (let post of posts) {%>
      <li class="list-group-item"><a href="/posts/<%=post.id%>" class="mr-2"><%=post.title%></a></li>
      <% } %>
    </ul>
  <% } else { %>
    <div class="alert alert-secondary" role="alert">
      No posts yet.
    </div>
  <% }} %>
</div>